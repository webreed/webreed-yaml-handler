{
  "name": "webreed-yaml-handler",
  "version": "0.0.5",
  "description": "YAML encoding/decoding handler plugin for webreed.",
  "repository": {
    "type": "git",
    "url": "https://github.com/webreed/webreed-yaml-handler"
  },
  "main": "lib/setup.js",
  "typings": "lib/setup.d.ts",
  "scripts": {
    "prepublish": "npm run build",
    "build": "npm run clean && tsc --pretty",
    "clean": "rm -rf lib",
    "test": "mocha ./test/spec/ --recursive"
  },
  "author": "Rotorz Limited",
  "license": "MIT",
  "dependencies": {
    "js-yaml": "^3.4.6",
    "webreed-core": "^0.0.x"
  },
  "devDependencies": {
    "merge2": "^1.0.1",
    "mocha": "^2.3.4",
    "mocha-testdata": "^1.1.2",
    "should": "^8.0.2",
    "typescript": "^1.8.10"
  }
}
